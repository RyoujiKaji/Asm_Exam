
```masm
.586 
.MODEL     flat, stdcall
OPTION     CASEMAP:NONE                   ;чувствительность к регистру 

Include C:\masm32\include\kernel32.inc
IncludeLib C:\masm32\lib\kernel32.lib

.CONST  
    Arr DB 11, 2, 3, 4, 5, 6
    SizeArr DB 6

.DATA? 
    Square DW 6 DUP (?)

.CODE 
Start:     
    ;Получить последовательность квадратов 6 чисел
    mov ebx, OFFSET Arr
    mov edi, OFFSET Square
    xor esi, esi 

    movzx ecx, BYTE PTR [SizeArr]
    For_start:
        mov al, [ebx + esi]
        mul al
        mov [edi + esi*2], ax
        
        inc esi
        loop For_start
    
    Invoke ExitProcess, 0              ;Выход с кодом 0 
End Start
```
