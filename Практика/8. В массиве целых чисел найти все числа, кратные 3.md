
```masm
.586 
.MODEL     flat, stdcall
OPTION     CASEMAP:NONE                   ;чувствительность к регистру 

Include    C:\masm32\include\kernel32.inc
IncludeLib C:\masm32\lib\kernel32.lib

.CONST  
    Arr DB 11, 2, 3, 4, 5, 0, 7, 8, 9, 10
    SizeArr DB 10

.CODE 
Start:     
    ;В массиве целых чисел найти все числа, кратные 3
    mov ebx, OFFSET Arr
    mov dl, 3

    movzx ecx, BYTE PTR [SizeArr]
    For_start:
        dec ecx
        mov al, [ebx + ecx]
        movsx di, al
        
        cbw
        idiv dl
        test ah, ah
        jnz Inc_for

        ;выводим di
        
    Inc_for:
        test ecx, ecx
        jnz For_start
    
    Invoke ExitProcess, 0              ;Выход с кодом 0 
End Start
```
